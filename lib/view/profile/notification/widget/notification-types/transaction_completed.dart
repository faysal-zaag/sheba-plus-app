import 'package:flutter/material.dart';

class TransactionCompleted extends StatelessWidget {
  const TransactionCompleted({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
