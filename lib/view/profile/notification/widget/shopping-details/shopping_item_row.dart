import 'package:flutter/material.dart';

class ShoppingItemRow extends StatelessWidget {
  const ShoppingItemRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
